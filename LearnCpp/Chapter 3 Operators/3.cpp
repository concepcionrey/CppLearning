#include <iostream>
// Chp 3
// Increment & Decrement operators & side effects
// 

// Incrementing & decrementing variables are very common to the point they have their
// own operators. Each type has 2 variants; a pre and a post version
// 1) pre-increments: ++x => increment x, then evaluate
// 2) pre-decrements: --x => decrement x, then evaluate
// 3) post-increments: x++ => evaluate x, then increment it
// 4) post-decrement: x-- => evaluate x, then decrement it
// 
// Rule: always favour using pre- ____ over post because of performance advantage
// and provides less confusion overall.

int main() {
return 0;
}